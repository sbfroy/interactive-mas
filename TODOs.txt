TODO: Decide whether Spock's context_brief should be a single prose block or lightly structured (e.g., "present: ...", "holding: ...", "recent: ..."). Content is settled — relevant chars/locs with names + one-line summaries, inventory highlights, current direction, recent commitments. Try prose first; fall back to light structure if Tolkien starts drifting.

TODO: Pick an i2v model and wire it in behind video_enabled=true. Confirm the end_frame_description → last-frame-as-image handoff works for the chosen model.

TODO: Pick an ElevenLabs voice for Attenborough and set elevenlabs_voice_id in configs. Confirm ~5s of audio lands cleanly for a ~1–3 sentence voiceover; tune max sentence length in the prompt if clips run long.

TODO: Refine the data/test_scenario.json. Should be properly written so that it accuractely can test the two worklows. Should for example be much longer.

TODO: When clips are generated in advance (and in general) at what time does this happen? Like is there a heartbeat every 5 seconds or just when the state graph has been run throug? Also since it can generate clips of varying sizes the 30 s buffer might converge to 0s. This could be a problem... How and when it generates is important to think about since it should just run autononomosly without user interaction, but adapt when user interacts.