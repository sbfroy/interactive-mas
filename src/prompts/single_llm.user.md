Turn: {turn_number}

Long-term narrative: {long_term_narrative}
Short-term narrative: {short_term_narrative}

World state (current truth):
{world_state}

Narrative memory (rolling):
{narrative_memory}

Context brief from the previous turn (may be empty):
{context_brief}

Previous clip's end-frame description (empty on turn 1):
{previous_end_frame_description}

Recent history (last ~5 turns, newest last — each entry has beat/shot/commentary):
{recent_history}

User input (may be empty — advance on short_term_narrative if so):
{user_input}

Produce the full output (beat + shot + commentary + memory_update) now, as a single JSON object.
